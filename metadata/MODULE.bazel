module(
    name = "package_metadata",
    version = "HEAD",  # Automatically updated by release pipeline.
)

# This is a fundamental module that's dependet on by virtually every bazel
# module and **MUST NOT** have any dependencies.
